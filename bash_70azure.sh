### This is kept here for historical purposes.
### We are now installing Azure CLI instead of using from a Docker container.
###
# alias azure-cli="docker run -it -v ${HOME}:/root azuresdk/azure-cli-python"

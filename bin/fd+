#!/bin/bash -eu

function fd+ {
    local distro="$(os_release | cut -d: -f1)"
    case "${distro}" in
        Debian|Ubuntu)   cmd=fdfind;;
        *) cmd=fd;;
    esac

    command -v "${cmd}" >/dev/null 2>&1 || apt+ install fd
    ${cmd} $@
}

fd+ $@

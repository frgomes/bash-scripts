#!/bin/bash -eu

function __plasma_stop() {
  command -v killall >/dev/null 2>&1 || apt+ install psmisc
  command -v plasmashell >/dev/null 2>&1 || apt+ install plasma-workspace
  killall plasmashell
}

__plasma_stop $*

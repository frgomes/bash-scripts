#!/bin/bash -e

function __kubectl_clusters {
  which jq  >/dev/null 2>&1 || sudo apt install jq -y
  which sed >/dev/null 2>&1 || sudo apt install sed -y
  cat ~/.kube/config | yaml2json | jq '.clusters[].name' | sed 's/"//g'
}

__kubectl_clusters $*

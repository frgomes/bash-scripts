#!/bin/bash -eu

function __kubectl_clusters {
  command -v jq  >/dev/null 2>&1 || apt+ install jq
  command -v sed >/dev/null 2>&1 || apt+ install sed
  cat ~/.kube/config | yaml2json | jq '.clusters[].name' | sed 's/"//g'
}

__kubectl_clusters $*
